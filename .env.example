# 复制本文件为 .env，并填入真实值。.env 不会被提交（见 .gitignore）。

# DeepSeek 服务端密钥（只在本地 Node 服务里使用，绝不暴露给浏览器）。
# 不填也能用：AI 不可用时会自动走本地 fallback，功能不会坏。
DEEPSEEK_API_KEY=sk-your-deepseek-key

# 可选：覆盖本地服务端口（默认 4823）。
# PORT=4823

# 可选：覆盖 DeepSeek 模型（默认 deepseek-chat）。
# DEEPSEEK_MODEL=deepseek-chat
